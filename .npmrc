# react-day-picker@8 peer-depends on date-fns ^2||^3 while the app pins date-fns 4.
# The v4 API is compatible for the formatters this app uses, so the resolution is
# forced rather than downgrading date-fns. Without this, a clean `npm install`
# fails outright with ERESOLVE.
legacy-peer-deps=true
